# Copy to .env.local (or set in your build environment) to point the
# download buttons somewhere other than the latest GitHub release.
# Each value may be a release page or a direct file URL.
VITE_DOWNLOAD_URL=
VITE_DOWNLOAD_URL_MACOS=
VITE_DOWNLOAD_URL_WINDOWS=

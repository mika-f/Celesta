[package]
name = "celesta-evaluator"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
celesta-composition = { path = "../composition" }
celesta-project = { path = "../project" }
